# Test builds

Not releases: builds handed out for testing.

- `mk64-adhoc-test.zip` (2026-09-10, build 3): ad hoc multiplayer test build from the
  `adhoc` branch (commit 802b348). Unzip and copy the `MK64Portable` folder
  to `ms0:/PSP/GAME/` (keep your existing ROM there). `ADHOC_TEST.txt`
  inside says what to try and which logs to send back.
